import { Probleme } from "./probleme";

export class AvancementActionProbleme {
    public id: number = Number(0);
    public update: string = String();
    public probleme = new Probleme();
    public topic: string = String();
}
