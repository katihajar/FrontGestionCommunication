import { PointVersion } from "./point-version";

export class PlanningPointVersion {
    public titre: string = String();
    public description: string = String();
    public pointVersion= new PointVersion();
}
