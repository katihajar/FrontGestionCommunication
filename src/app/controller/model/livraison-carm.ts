import { PointVersion } from "./point-version";

export class LivraisonCARM {
    public nombreTicket: number = Number(0);
    public statutMEI: string = String();
    public dateMEI= new Date();
    public pointVersion= new PointVersion();
}
